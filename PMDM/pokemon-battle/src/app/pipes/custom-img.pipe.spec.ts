import { CustomImgPipe } from './custom-img.pipe';

describe('CustomImgPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomImgPipe();
    expect(pipe).toBeTruthy();
  });
});
